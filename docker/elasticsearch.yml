# Elasticsearch 配置
cluster.name: history_corpus_es
node.name: node-1
node.data: true
node.master: true
network.host: 0.0.0.0
http.port: 9200
transport.port: 9300

# 索引设置
index.number_of_shards: 1
index.number_of_replicas: 0

# 日志
logger.level: INFO
